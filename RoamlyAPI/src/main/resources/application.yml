spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/roamly
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update

server:
  error:
    include-message: always
  #    include-exception: true

